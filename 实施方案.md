# 占卜算命网站实施方案（Python 后端 + MySQL + 大模型）

## 1. 项目目标与边界

### 1.1 目标
构建一个本地先行、后续可部署到 Ubuntu 的占卜平台，支持以下功能：
- 八字命理（四柱八字）
- 六爻占卜
- 姓名学（五格剖象）
- 塔罗
- 抽签（通用）
- 观音灵签
- 月老灵签

并实现：
- 占卜全过程结构化存档到 MySQL
- 大模型辅助“解释层”，但核心推演逻辑可追溯、可复算
- 知识库可引用来源，避免“瞎编”

### 1.2 原则（保证“玄学可靠性”的工程化做法）
1. 规则优先：
   先用经典规则引擎给出“可复盘结论”，再由大模型润色解释。
2. 可追溯：
   每次占卜保留：输入参数、随机种子、规则命中路径、引用条目、模型提示词版本。
3. 引用可核验：
   解释文本中尽量附“依据条目 ID + 来源书目”。
4. 版本化：
   规则库、签文库、塔罗牌义库都要有版本号，避免结果漂移不可控。
5. 风险控制：
   明确“娱乐与民俗文化参考”，禁止生成医疗/法律/投资等确定性承诺。

---

## 2. 技术架构（本地开发优先）

### 2.1 推荐技术栈
- 后端：Python 3.11 + FastAPI
- 任务队列（可选）：Celery + Redis（后续并发时再加）
- 数据库：MySQL 8.0
- ORM：SQLAlchemy 2.x + Alembic
- 向量检索（可选）：
  - 本地轻量：SQLite + FAISS
  - 或 MySQL 向量插件（后期）
- 前端：先用简易模板（Jinja2）或分离式前端都可
- 部署：
  - 本地：Windows + uvicorn
  - 线上：Ubuntu + Nginx + Gunicorn/Uvicorn

### 2.2 分层设计
1. API 层
- 接收请求、参数校验、鉴权、限流

2. 占卜引擎层（核心）
- 各模块规则计算器：
  - bazi_engine
  - liuyao_engine
  - name_wuge_engine
  - tarot_engine
  - lot_engine（签文）

3. 知识层
- 结构化规则表
- 文本知识库（经典条文 + 现代注释）
- 检索服务（关键词/向量召回）

4. 解释层（LLM）
- 将规则结论 + 知识条目拼装成提示词
- 输出“解释文本 + 依据列表”

5. 持久化层
- MySQL 存用户、会话、占卜记录、规则命中、模型调用日志

---

## 3. 目录结构建议（首版）

```text
Divination/
  docs/
    占卜系统实施方案.md
    数据字典.md
    知识库来源清单.md
  backend/
    app/
      main.py
      core/
        config.py
        security.py
        logging.py
      api/
        v1/
          bazi.py
          liuyao.py
          name.py
          tarot.py
          lots.py
          history.py
      domain/
        bazi/
        liuyao/
        name_wuge/
        tarot/
        lot/
      services/
        llm_service.py
        kb_service.py
        trace_service.py
      models/
        user.py
        divination_session.py
        divination_record.py
        rule_hit.py
        kb_entry.py
        llm_call_log.py
      db/
        base.py
        session.py
      schemas/
      utils/
    alembic/
    requirements.txt
    .env.example
  frontend/
    (后续)
```

---

## 4. 占卜模块设计（功能完整 + 可追溯）

## 4.1 八字命理（四柱八字）

### 输入
- 公历出生时间（精确到分钟，建议）
- 出生地（用于时区/真太阳时修正，可选）
- 性别（传统流派常用）

### 规则计算流程
1. 公历转干支历：年柱、月柱、日柱、时柱
2. 藏干、十神、五行强弱统计
3. 旺衰判断（按选定流派）
4. 格局识别（正格/从格等，首版可先做常见格局）
5. 喜用神推断（规则可配置）
6. 大运/流年计算（起运岁数、顺逆排）
7. 输出结构化结论

### 结果输出建议
- 命盘结构：四柱、十神分布、五行统计图
- 核心结论：日主强弱、喜用神、忌神
- 主题解读：性格、事业、财运、感情、健康（仅民俗建议）
- 时间线：近 3-5 年流年重点
- 依据：命中规则编号 + 经典条文引用

### 可靠性关键点
- 使用公开可验证历法库或自研校验单元测试
- 所有“结论句”关联具体规则 ID

## 4.2 六爻占卜

### 输入
- 起卦方式：
  - 手动输入（六次投掷结果）
  - 系统随机（需记录随机种子）
- 问题类型（感情/事业/学业等）
- 起卦时间

### 规则计算流程
1. 生成本卦、变卦
2. 标记动爻
3. 装卦（纳甲、六亲、六神、世应）
4. 用神判定（按问题类型映射）
5. 生克冲合、月日旺衰分析
6. 生成结构化判断

### 结果输出建议
- 卦象：本卦/变卦名、卦辞爻辞
- 关键爻解释：动爻、世应关系
- 吉凶倾向与时间建议（区间表达，不给绝对承诺）
- 依据：卦辞爻辞来源 + 规则命中

### 可靠性关键点
- 随机起卦必须可复现（seed + 算法版本）
- 用神映射表明确且可配置

## 4.3 姓名学（五格剖象）

### 输入
- 姓名（简体/繁体策略要明确）
- 性别（可选）

### 规则计算流程
1. 统一字形与笔画来源（必须固定版本字典）
2. 计算天格、人格、地格、外格、总格
3. 数理吉凶映射（81 数理）
4. 三才配置分析
5. 输出结构化建议

### 可靠性关键点
- 一定要锁定“笔画字典版本”，并向用户展示
- 简繁转换规则要统一，否则结果会漂移

## 4.4 塔罗

### 输入
- 问题类型
- 牌阵选择（单张/三张/凯尔特十字等）
- 是否允许逆位

### 规则计算流程
1. 洗牌与抽牌（记录 seed）
2. 牌位语义映射
3. 正逆位牌义提取
4. 跨牌组合关系（冲突/互补）
5. 生成解释草稿

### 可靠性关键点
- 牌义库要有来源版本（不同塔罗体系差异大）
- 解读应注明“牌位 + 正逆位 + 牌义来源”

## 4.5 抽签（通用）

### 输入
- 签库类型（通用/自定义）
- 问题类型

### 规则计算流程
1. 随机抽签（记录 seed）
2. 返回签号、签文、解签
3. 结合问题类型输出建议

## 4.6 观音灵签 / 月老灵签

### 输入
- 问题描述
- 可选：求签日期

### 规则计算流程
1. 抽签得到签号
2. 从签文库读取：签诗、典故、传统解签、现代解释模板
3. 结合用户问题生成个性化解释

### 可靠性关键点
- 签文与解签要来自稳定版本文本
- 每条解释附签号与来源

---

## 5. 大模型接入方案（你给的接口可直接用）

已知配置：
- BASE_URL: http://103.239.152.247:8022/v1
- MODEL: qwen3.5-27b-fp8
- API_KEY: 已提供

### 5.1 强烈建议
不要把密钥硬编码进仓库，改为环境变量：
- OPENAI_BASE_URL
- OPENAI_MODEL
- OPENAI_API_KEY

### 5.2 调用策略
1. 规则先行：
- 先跑规则引擎得到结构化结果 JSON

2. 检索增强：
- 从知识库召回 top-k 条依据文本

3. 提示词模板：
- System：角色、风格、风险边界、禁止编造
- User：问题 + 规则结果 + 依据条目

4. 输出格式约束：
- JSON 格式返回：
  - summary
  - details
  - evidence_ids
  - uncertainty
  - advice

### 5.3 防瞎编机制
- 要求模型“仅可使用提供依据推理”
- 若依据不足，返回“信息不足”并提示补充
- 对输出做后处理校验：
  - evidence_ids 必须都存在
  - 不允许出现医疗/法律确定性断言

---

## 6. MySQL 数据库设计（全过程留痕）

## 6.1 核心表

1. users
- id
- nickname
- created_at

2. divination_sessions
- id
- user_id
- category（bazi/liuyao/name/tarot/lot）
- question
- client_meta（IP/UA）
- created_at

3. divination_records
- id
- session_id
- input_payload（原始输入）
- calc_result（规则引擎结构化结果）
- final_text（最终展示文案）
- confidence_level（低/中/高）
- created_at

4. rule_hits
- id
- record_id
- module
- rule_code
- rule_version
- hit_context
- created_at

5. kb_entries
- id
- kb_type（classic/book/sign/tarot）
- title
- content
- source_name
- source_ref
- tags
- version
- created_at

6. record_evidences
- id
- record_id
- kb_entry_id
- quote_text

7. llm_call_logs
- id
- record_id
- provider
- model
- prompt_tokens
- completion_tokens
- latency_ms
- request_payload_masked
- response_payload
- created_at

8. random_traces
- id
- record_id
- rng_algorithm
- seed
- draw_steps

## 6.2 索引建议
- divination_sessions(user_id, created_at)
- divination_records(session_id, created_at)
- rule_hits(record_id, module)
- kb_entries(kb_type, tags)
- record_evidences(record_id, kb_entry_id)

---

## 7. 知识库建设方案（权威性优先）

## 7.1 数据分层
1. 原典层（高优先）
- 八字、六爻、签文相关经典原文或可靠整理版本

2. 注释层
- 学术出版物、长期流传注解、可信社区整理

3. 现代解释层
- 面向用户可读性的白话解释（需标注“现代释义”）

## 7.2 入库字段规范
- source_name：来源名
- source_type：古籍/出版社/网站
- source_url_or_isbn
- editor_note：清洗说明
- reliability_level：A/B/C
- copyright_note：版权状态

## 7.3 质量控制
- 每条知识至少一个来源
- 关键结论至少双来源交叉验证
- 对存在流派冲突的条目打标签（school=xxx）

---

## 8. 你需要补充给我的资源清单（最关键）

为了避免“瞎编”，下面资源请你优先收集，我再帮你做清洗入库脚本：

1. 八字规则资料
- 干支历转换校验样例（至少 50 组）
- 十神/格局/喜用神判定规则文本（明确流派）

2. 六爻资料
- 卦名、爻辞、纳甲映射、六亲六神规则
- 用神判定表（按问题类型）

3. 姓名学资料
- 笔画字典（简体/繁体版本说明）
- 81 数理吉凶与三才配置表

4. 塔罗资料
- 78 张牌牌义（正位/逆位）
- 至少一种固定牌阵说明

5. 签文资料
- 观音灵签完整签号、签诗、典故、传统解签
- 月老灵签完整签号、签诗、解签

6. 版权与来源
- 允许商用或可合理引用的来源优先
- 每份资料保留原始来源链接或书目信息

---

## 9. API 设计草案（首版）

- POST /api/v1/bazi/reading
- POST /api/v1/liuyao/reading
- POST /api/v1/name/reading
- POST /api/v1/tarot/reading
- POST /api/v1/lots/guanyin/reading
- POST /api/v1/lots/yuelao/reading
- GET /api/v1/history/{session_id}

统一返回：
- reading_id
- module
- result_structured
- result_text
- evidences
- trace_id

---

## 10. 开发里程碑（建议 4 周）

### 第 1 周：骨架
- FastAPI + MySQL + Alembic
- 用户、会话、记录、日志表
- 抽签模块（最先跑通全链路）

### 第 2 周：规则引擎
- 八字基础排盘
- 六爻起卦与基础解读
- 姓名五格计算

### 第 3 周：知识库 + LLM
- KB 清洗入库脚本
- 检索增强
- LLM 解释模板 + 输出校验

### 第 4 周：完善与部署
- 塔罗模块
- 历史记录查询
- Ubuntu 部署脚本 + Nginx 反向代理
- 压测与风控

---

## 11. 本地开发与部署建议

### 11.1 本地（Windows）
- 使用 .env 管理密钥
- 启动 MySQL 8
- alembic upgrade head
- uvicorn app.main:app --reload

### 11.2 Ubuntu 部署（后续）
- Python 虚拟环境 + systemd 管理进程
- Nginx 反代 + HTTPS
- MySQL 单独主机或同机
- 日志采集（按天切分）

---

## 12. 合规与风险提示文案（建议前端固定展示）

- 本服务为民俗文化与娱乐参考，不构成医疗、法律、投资等专业建议。
- 结果受输入信息完整度与流派差异影响，请理性看待。
- 涉及个人隐私的数据将按最小必要原则存储。

---

## 13. 下一步我可以立即帮你做的事

1. 生成可运行的 FastAPI + MySQL 项目骨架（含 Alembic 与基础表）
2. 先落地“观音灵签 + 月老灵签”全链路（最容易验证完整闭环）
3. 写知识库导入脚本模板（CSV/JSON 入库 + 来源元数据）
4. 给你一份可直接采集资料的 Excel 字段模板

如果你同意，我下一步就直接在本地把项目骨架和第一批 API 写出来。